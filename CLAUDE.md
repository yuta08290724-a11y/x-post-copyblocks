# x-post-copyblocks

`x-post-daily-core` が毎朝生成するコピーブロックHTML（`x-copy-blocks-{YYYY-MM-DD}.html`）をGitHub Pagesで公開するための、**公開専用リポジトリ**です。ビルド・テスト・依存関係は一切なく、静的HTMLをコミットして配信するだけの構成になっています。

- リポジトリ: `yuta08290724-a11y/x-post-copyblocks`
- 既定ブランチ: `main`（GitHub Pagesの配信元）
- 公開URLの形: `https://yuta08290724-a11y.github.io/x-post-copyblocks/x-copy-blocks-{YYYY-MM-DD}.html`

## リポジトリ構成

```
CLAUDE.md                        このファイル
index.html                       Pagesのルート。案内文だけの最小ページ（日次ファイルへのリンクは無い）
x-copy-blocks-2026-07-04.html    日次の生成物（1日1ファイル、追記のみ・過去分は消さない）
...
x-copy-blocks-2026-07-31.html
```

- ディレクトリ階層は無し。すべてリポジトリ直下のフラット構成。
- CI／GitHub Actions／`_config.yml` などの設定ファイルは存在しない（Pagesの素の静的配信）。
- 日次ファイルは毎日必ず存在するわけではなく、欠番日がある（例: 07-24, 07-25, 07-30）。欠番を「壊れている」と判断して埋めに行かないこと。
- 過去日のファイルは履歴として残す。整理・削除・リネームは**明示的な依頼がない限り行わない**。

## 日次HTMLの構造（生成物のフォーマット）

各 `x-copy-blocks-{日付}.html` は依存ゼロの自己完結HTML（インラインCSS＋インラインJS、外部読み込み無し、`lang="ja"`）。全日で共通の骨格を持つ:

- `<header class="page-header">` — `<h1>` タイトル、`.date` に日付、`.rank-line` に推奨順位（例: `推奨順位　1位=No.4 / 2位=No.3 / ...`）。
- `.cards` 配下に `.card` を並べる。各カードは
  - `data-card="{key}"` と、`No.x` バッジ／投稿時刻バッジ（`badge-time`）／監査点数バッジ（`badge-score`）／文字数バッジ（`badge-count`）
  - `data-body="{key}"` の空の本文要素（**HTMLには本文を書かない**）
  - `data-copy-btn="{key}"` のコピーボタン
- 末尾の `<script>` 内 IIFE に `var POSTS = { "{key}": \`本文\`, ... }` としてテキストを保持し、DOM構築時に `data-body` へ流し込み、同時に `text.length + "字"` を文字数バッジへ反映する。
- コピー処理は `navigator.clipboard.writeText` を第一手段、失敗時は隠し `<textarea>` ＋ `document.execCommand("copy")` にフォールバック。成功で「✓ コピーしました」、失敗で「コピー失敗」を1.5秒表示。
- 配色はルート変数（`--green-deep` `--green-mid` `--orange` など）で定義された和のベージュ／グリーン系。No.5系カードは `.accent-recipe`（オレンジの左罫）で区別。

**重要**: `POSTS` のキーとカードの `data-card` / `data-body` / `data-count-for` / `data-copy-btn` は完全に一致している必要がある。キーを増減させる編集をするときは4か所すべてを揃えること。

### コンテンツの2世代（2026-07-18で切り替わり）

同じ骨格のまま中身が変わっているので、日付で見分ける。

| | 〜2026-07-17 | 2026-07-18〜 |
|---|---|---|
| タイトル / `<h1>` | `X投稿 コピーブロック` | `X図解 画像プロンプト` |
| `POSTS` の中身 | Xにそのまま貼る投稿本文（関西弁の語り口・レシピ本文） | 画像生成AIに渡すプロンプト文 |
| No.5 | 単一カード（キー `"5"`） | 3分割カード（キー `"5a"` `"5b"` `"5c"`、バッジは `No.5-①〜③`） |
| フッター | `タップでコピー → Xに貼り付けて投稿` | `タップでコピー → 画像生成AIに貼り付け（シェフ入りは official-chef.jpg を添付）` |

新規ファイルを扱う際は最新日のファイル（現時点で `x-copy-blocks-2026-07-31.html`）を基準にする。

## 編集・コミットの流儀

- 生成物は原則 `x-post-daily-core` 側が作る。このリポジトリでは**手直しと公開**だけを行う。
- 修正はHTMLを直接編集する。テンプレートエンジンもビルドステップも無いので、編集した内容がそのまま公開物になる。
- 検証はブラウザでファイルを開くだけ（`file://` でも動作する）。実行するテストは無い。
- コミットメッセージは日本語または英語の短い prefix 付きで、既存の慣習に合わせる:
  - `post: 2026-07-31` — その日の新規ファイル追加（自動投入分の定型）
  - `update: No.5を3パネル図解構成に変更 (2026-07-18)` — 既存日ファイルの内容変更
  - `fix: ...` / `retry: デプロイ再試行` / `docs: ...`
- 1コミット1日分が基本。過去日ファイルをまとめて書き換えない。

## URL回収ルーチン（計測欄への書き戻し）

ユーザーがこのリポジトリの新規セッションで、その日投稿したX投稿のURLを貼った場合（目安：5本前後のURLがまとまって貼られる）、Google Calendar（`mcp__Google_Calendar__*` ツール、対象カレンダーは `yuta08290724@gmail.com`）の該当イベントのdescription内「計測：」行に、貼られたURLを書き戻す。

### 対象日の確定手順（重要：投稿日を「URLを貼った当日」に固定しない）

深夜に前日分のURLを貼るケースがあるため、対象日は必ず以下の手順で確定する。決して「今日の日付」を無条件に採用しない。

1. Google Calendarで「当日」と「前日」の2日分の予定を取得する。
2. 各日について、`📝 X No.1`〜`📝 X No.5` の5件が揃っているか確認する。
3. 5件揃っている日のうち、descriptionの「計測：」行が未記録（例：「計測：URL未記録のため計測不可」等、実URLが入っていない状態）になっている日を「候補日」とする。
4. 候補日が **1日だけ** の場合：その日を対象日として確定し、貼られたURLをNo.1→No.2→No.3→No.4→No.5の順に、上から貼られた順番でそれぞれの「計測：」行に書き込む。
5. 候補日が **2日以上** ある場合、または **0日**（5件揃っていない／全て記録済みなど一意に決められない）の場合：Calendarへの更新は一切行わず、ユーザーに「何月何日分のURLですか？」と確認する。ユーザーの回答を得てから、その日付のNo.1〜No.5に上から順に書き込む。

### 書き込み時の注意

- 既存の「計測：」行の前半（監査点数や区切り）は変更せず、URL未記録部分のみをURLに置き換える。
- 貼られたURLの本数がNo.1〜No.5の件数（5件）と一致しない場合も、対象日を確定させず先にユーザーに確認する。
- 更新には `mcp__Google_Calendar__update_event` を使い、descriptionの他の行（監査行、コピー本文など）は書き換えない。

## 補足

- HTMLの日付（`.date`）・ファイル名の日付・カレンダー上の投稿日は同じ日を指す。ファイル名だけ直して中身の日付を残す、といったズレを作らない。
- カード内の投稿時刻（11:00 / 14:00 / 17:00 / 19:00 / 18:00 など）と監査点数はCalendar側の予定と対応する情報なので、片方だけを勝手に書き換えない。
- 生成ロジックそのもの（プロンプト設計・点数付け・順位付け）は `x-post-daily-core` 側の責務。このリポジトリで再実装しない。
